import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.projects.app',
  appName: 'converter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
